"""setup.py - Legacy setup file for litfit.

This file exists primarily for older pip versions that don't fully support
pyproject.toml. The main configuration is in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
